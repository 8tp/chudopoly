# Comma-separated extra browser origins allowed to open WebSockets in production.
# Same-origin browser connections are always accepted.
ALLOWED_ORIGINS=https://example.com

# Optional GIF search integration.
GIPHY_KEY=

# Hosting platforms normally inject this value.
PORT=3000

# How many X-Forwarded-For hops are trusted infrastructure, counted from the
# RIGHT of the chain. Default 1: the production deploy is Railway single-hop,
# whose edge appends the real client IP after anything the client sent — so
# the rightmost entry is trustworthy and the leftmost is attacker input.
# 0 = ignore XFF entirely (use the socket's remoteAddress; correct only when
# the process is directly exposed with no proxy in front). The per-IP socket
# cap and the /api/public-rooms rate limiter both key on this.
# CHUD_TRUST_PROXY_HOPS=1
